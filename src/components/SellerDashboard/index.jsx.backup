import { useState } from "react";
import { useSelector } from "react-redux";

export default function SellerDashboard() {
  const { user } = useSelector((state) => state.auth);
  const [activeSection, setActiveSection] = useState("dashboard");
  const [searchQuery, setSearchQuery] = useState("");
  const [sidebarCollapsed, setSidebarCollapsed] = useState(false);

  // Sample product data
  const [products] = useState([
    {
      id: "PROD-024",
      name: "UPPAbaby Vista V2 Stroller",
      customer: "Dennis Lau",
      email: "dennislau@gmail.com",
      phone: "+1 234-567-8901",
      marketplace: "Shopee",
      orders: 106,
      totalSpent: 2450,
      status: "Active"
    },
    {
      id: "PROD-024",
      name: "Chicco KeyFit 30 Car Seat",
      customer: "Edward Williams",
      email: "edwardwilliams@gmail.com",
      phone: "+1 345-678-9012",
      marketplace: "Tokopedia",
      orders: 124,
      totalSpent: 3456,
      status: "Active"
    },
    {
      id: "PROD-024",
      name: "Fisher-Price Rock 'n Play",
      customer: "Sam Davis",
      email: "samdavis@gmail.com",
      phone: "+1 456-789-0123",
      marketplace: "Amazon",
      orders: 131,
      totalSpent: 4567,
      status: "Active"
    },
    {
      id: "PROD-024",
      name: "Baby Bjorn Carrier",
      customer: "Larry Smith",
      email: "larrysmith@gmail.com",
      phone: "+1 567-890-1234",
      marketplace: "TikTok Shop",
      orders: 90,
      totalSpent: 5678,
      status: "Inactive"
    },
    {
      id: "PROD-024",
      name: "Graco Pack 'n Play",
      customer: "Kenneth Lau",
      email: "kennethlau@gmail.com",
      phone: "+1 678-901-2345",
      marketplace: "Shopee",
      orders: 189,
      totalSpent: 6789,
      status: "Blocked"
    }
  ]);

  // Stats data
  const stats = {
    totalProducts: { value: "104,012", change: "+4.3%", increased: 1438, period: "this week" },
    newProducts: { value: "1,203", change: "+12.5%", increased: 467, period: "this week" },
    avgOrderValue: { value: "$156.78", change: "+0.3%", decreased: 2.2, period: "this week" },
    satisfaction: { value: "4.8", change: "/5", increased: 2.3, period: "this week" }
  };

  const menuItems = [
    { id: "dashboard", icon: "", label: "Dashboard" },
    { id: "myorder", icon: "", label: "My Order" },
    { id: "sales", icon: "", label: "Sales Performances" },
    { id: "messages", icon: "", label: "Messages" }
  ];

  const managementItems = [
    { id: "customer", icon: "", label: "Customer" },
    { id: "product", icon: "", label: "Product" },
    { id: "marketing", icon: "", label: "Marketing" },
    { id: "shipping", icon: "", label: "Shipping" },
    { id: "finance", icon: "", label: "Finance" },
    { id: "marketplace", icon: "", label: "Connected Marketplace" }
  ];

  const getMarketplaceLogo = (marketplace) => {
    const logos = {
      "Shopee": "",
      "Tokopedia": "",
      "Amazon": "",
      "TikTok Shop": ""
    };
    return logos[marketplace] || "";
  };

  const getStatusColor = (status) => {
    const colors = {
      "Active": "text-success-green bg-green-50",
      "Inactive": "text-gray-600 bg-gray-100",
      "Blocked": "text-red-600 bg-red-50"
    };
    return colors[status] || "text-gray-600 bg-gray-100";
  };

  return (
    <Layout childrenClasses="pt-0 pb-0">
      <div className="seller-dashboard-wrapper w-full bg-off-white min-h-screen">
        <div className="flex h-screen">
          <div className={`sidebar ${sidebarCollapsed ? 'collapsed' : ''}`}>
            <div className="logo">
              <img src={`${process.env.PUBLIC_URL}/assets/images/mobileLogo.png`} alt="Logo" />
            </div>
            <div className="menu">
              {menuItems.map((item) => (
                <button
                  key={item.id}
                  onClick={() => setActiveSection(item.id)}
                  className={`menu-item ${activeSection === item.id ? 'active' : ''}`}
                >
                  <span className="icon">{item.icon}</span>
                  <span className="label">{item.label}</span>
                </button>
              ))}
            </div>
            <div className="management">
              <h3 className="title">Management</h3>
              {managementItems.map((item) => (
                <button
                  key={item.id}
                  className="menu-item"
                >
                  <span className="icon">{item.icon}</span>
                  <span className="label">{item.label}</span>
                </button>
              ))}
            </div>
          </div>
          <div className="content">
            <div className="header">
              <button
                className="toggle-sidebar"
                onClick={() => setSidebarCollapsed(!sidebarCollapsed)}
              >
                <span className="icon"></span>
              </button>
              <div className="search">
                <input
                  type="search"
                  value={searchQuery}
                  onChange={(e) => setSearchQuery(e.target.value)}
                  placeholder="Search..."
                />
              </div>
            </div>
            <div className="stats">
              {Object.keys(stats).map((stat) => (
                <div
                  key={stat}
                  className="stat-card"
                >
                  <h3 className="title">{stat}</h3>
                  <p className="value">{stats[stat].value}</p>
                  <p className="change">{stats[stat].change}</p>
                  <p className="period">{stats[stat].period}</p>
                </div>
              ))}
            </div>
            <div className="products">
              <h2 className="title">Products</h2>
              <table className="table">
                <thead>
                  <tr>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Customer</th>
                    <th>Email</th>
                    <th>Phone</th>
                    <th>Marketplace</th>
                    <th>Orders</th>
                    <th>Total Spent</th>
                    <th>Status</th>
                  </tr>
                </thead>
                <tbody>
                  {products.map((product) => (
                    <tr key={product.id}>
                      <td>{product.id}</td>
                      <td>{product.name}</td>
                      <td>{product.customer}</td>
                      <td>{product.email}</td>
                      <td>{product.phone}</td>
                      <td>{getMarketplaceLogo(product.marketplace)}</td>
                      <td>{product.orders}</td>
                      <td>${product.totalSpent}</td>
                      <td className={getStatusColor(product.status)}>{product.status}</td>
                    </tr>
                  ))}
                </tbody>
              </table>
            </div>
          </div>
          <div className="title mb-8">
            <PageTitle
              title="Seller Dashboard - Toddler Kingdom"
              breadcrumb={[
                { name: "home", path: "/home-one" },
                { name: "Seller Dashboard", path: "/seller-dashboard" },
              ]}
            />
          </div>

          <div className="content-wrapper w-full pb-10">
            <div className="container-x mx-auto">
              {/* Welcome Header */}
              <div className="bg-gradient-to-r from-primary-blue to-blue-500 rounded-2xl p-8 mb-8 text-white shadow-lg">
                <div className="flex items-center justify-between">
                  <div>
                    <h1 className="text-3xl font-bold mb-2">Seller Dashboard</h1>
                    <p className="text-blue-100">Manage your listings and track your sales</p>
                  </div>
                  <div className="hidden md:block">
                    <img
                      src={`${process.env.PUBLIC_URL}/assets/images/mobileLogo.png`}
                      alt="Logo"
                      className="h-16 w-16 object-contain"
                    />
                  </div>
                </div>
              </div>

              {/* Stats Cards */}
              <div className="grid grid-cols-1 md:grid-cols-4 gap-6 mb-8">
                {/* Total Earnings */}
                <div className="bg-white rounded-xl p-6 shadow-md border-l-4 border-success-green transform transition-all duration-300 hover:scale-105 hover:shadow-lg">
                  <div className="flex items-center justify-between mb-2">
                    <p className="text-gray-500 text-sm uppercase font-medium">Total Earnings</p>
                    <div className="w-10 h-10 rounded-full bg-green-100 flex items-center justify-center">
                      <svg xmlns="http://www.w3.org/2000/svg" className="h-5 w-5 text-success-green" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
                      </svg>
                    </div>
                  </div>
                  <h3 className="text-3xl font-bold text-gray-800">${totalEarnings}</h3>
                </div>

                {/* Active Listings */}
                <div className="bg-white rounded-xl p-6 shadow-md border-l-4 border-primary-blue transform transition-all duration-300 hover:scale-105 hover:shadow-lg">
                  <div className="flex items-center justify-between mb-2">
                    <p className="text-gray-500 text-sm uppercase font-medium">Active Listings</p>
                    <div className="w-10 h-10 rounded-full bg-blue-100 flex items-center justify-center">
                      <svg xmlns="http://www.w3.org/2000/svg" className="h-5 w-5 text-primary-blue" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4" />
                      </svg>
                    </div>
                  </div>
                  <h3 className="text-3xl font-bold text-gray-800">{activeListings}</h3>
                </div>

                {/* Total Views */}
                <div className="bg-white rounded-xl p-6 shadow-md border-l-4 border-peachy-pink transform transition-all duration-300 hover:scale-105 hover:shadow-lg">
                  <div className="flex items-center justify-between mb-2">
                    <p className="text-gray-500 text-sm uppercase font-medium">Total Views</p>
                    <div className="w-10 h-10 rounded-full bg-pink-100 flex items-center justify-center">
                      <svg xmlns="http://www.w3.org/2000/svg" className="h-5 w-5 text-peachy-pink" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                        <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
                        <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z" />
                      </svg>
                    </div>
                  </div>
                  <h3 className="text-3xl font-bold text-gray-800">{totalViews}</h3>
                </div>

                {/* Seller Rating */}
                <div className="bg-white rounded-xl p-6 shadow-md border-l-4 border-yellow-400 transform transition-all duration-300 hover:scale-105 hover:shadow-lg">
                  <div className="flex items-center justify-between mb-2">
                    <p className="text-gray-500 text-sm uppercase font-medium">Seller Rating</p>
                    <div className="w-10 h-10 rounded-full bg-yellow-100 flex items-center justify-center">
                      <svg xmlns="http://www.w3.org/2000/svg" className="h-5 w-5 text-yellow-500" fill="currentColor" viewBox="0 0 24 24">
                        <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z" />
                      </svg>
                    </div>
                  </div>
                  <h3 className="text-3xl font-bold text-gray-800">{sellerRating}</h3>
                </div>
              </div>

              {/* Tabs */}
              <div className="bg-white rounded-2xl shadow-lg overflow-hidden">
                <div className="flex border-b border-gray-200">
                  <button
                    onClick={() => setActiveTab("overview")}
                    className={`flex-1 px-6 py-4 font-semibold transition-all ${activeTab === "overview"
                      ? "bg-primary-blue text-white"
                      : "bg-white text-medium-grey hover:bg-gray-50"
                      }`}
                  >
                    Overview
                  </button>
                  <button
                    onClick={() => setActiveTab("products")}
                    className={`flex-1 px-6 py-4 font-semibold transition-all ${activeTab === "products"
                      ? "bg-primary-blue text-white"
                      : "bg-white text-medium-grey hover:bg-gray-50"
                      }`}
                  >
                    My Products
                  </button>
                  <button
                    onClick={() => setActiveTab("messages")}
                    className={`flex-1 px-6 py-4 font-semibold transition-all relative ${activeTab === "messages"
                      ? "bg-primary-blue text-white"
                      : "bg-white text-medium-grey hover:bg-gray-50"
                      }`}
                  >
                    Messages
                    {messages.filter(m => m.unread).length > 0 && (
                      <span className="absolute top-2 right-2 bg-red-500 text-white text-xs rounded-full w-5 h-5 flex items-center justify-center">
                        {messages.filter(m => m.unread).length}
                      </span>
                    )}
                  </button>
                </div>

                <div className="p-8">

                  {/* Overview Tab */}
                  {activeTab === "overview" && (
                    <div>
                      <div className="mb-8">
                        <h2 className="text-2xl font-bold text-qblack mb-2">Your Listings</h2>
                        <p className="text-sm text-medium-grey">Quick view of your active products</p>
                      </div>

                      {/* Product Grid */}
                      <div className="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
                        {products.filter(p => p.verificationStatus === 'approved').map((product) => (
                          <div
                            key={product.id}
                            className="bg-white border border-gray-200 rounded-xl overflow-hidden shadow-sm hover:shadow-xl transition-all transform hover:-translate-y-1"
                          >
                            <div className="h-48 bg-gray-100 overflow-hidden">
                              <img
                                src={`${process.env.PUBLIC_URL}${product.images[0]}`}
                                alt={product.name}
                                className="w-full h-full object-cover transition-transform duration-300 hover:scale-110"
                                onError={(e) => {
                                  e.target.src = `${process.env.PUBLIC_URL}/assets/images/product-img-1.jpg`;
                                }}
                              />
                            </div>
                            <div className="p-5">
                              <h3 className="font-bold text-qblack mb-2 line-clamp-2">{product.name}</h3>
                              <div className="flex items-center justify-between">
                                <span className="text-2xl font-bold text-primary-blue">${product.price}</span>
                                <div className="flex items-center gap-2 text-sm text-gray-500">
                                  <svg xmlns="http://www.w3.org/2000/svg" className="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M15 12a3 3 0 11-6 0 3 3 0 016 0z" />
                                    <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z" />
                                  </svg>
                                  <span>{product.views || 0}</span>
                                </div>
                              </div>
                            </div>
                          </div>
                        ))}
                      </div>

                      {/* Recent Messages Preview */}
                      <div className="bg-gradient-to-br from-accent-cream to-white rounded-xl p-6 border border-gray-200">
                        <div className="flex items-center justify-between mb-4">
                          <h3 className="text-xl font-bold text-qblack">Recent Messages</h3>
                          <button
                            onClick={() => setActiveTab('messages')}
                            className="text-primary-blue text-sm font-medium hover:underline"
                          >
                            View All →
                          </button>
                        </div>
                        <div className="space-y-3">
                          {messages.slice(0, 2).map((msg) => (
                            <div
                              key={msg.id}
                              className={`bg-white rounded-lg p-4 border ${msg.unread ? 'border-primary-blue' : 'border-gray-200'
                                }`}
                            >
                              <div className="flex items-start justify-between mb-2">
                                <div className="flex items-center gap-3">
                                  <div className="w-10 h-10 rounded-full bg-primary-blue text-white flex items-center justify-center font-semibold">
                                    {msg.sender.charAt(0)}
                                  </div>
                                  <div>
                                    <p className="font-semibold text-qblack">{msg.sender}</p>
                                    <p className="text-xs text-gray-500">{msg.time}</p>
                                  </div>
                                </div>
                                {msg.unread && (
                                  <span className="bg-red-500 text-white text-xs px-2 py-1 rounded-full">New</span>
                                )}
                              </div>
                              <p className="text-sm text-medium-grey line-clamp-2">{msg.message}</p>
                            </div>
                          ))}
                        </div>
                      </div>
                    </div>
                  )}

                  {/* Products Tab */}
                  {activeTab === "products" && (
                    <div>
                      <div className="flex justify-between items-center mb-8">
                        <div>
                          <h2 className="text-2xl font-bold text-qblack mb-1">Product Management</h2>
                          <p className="text-sm text-medium-grey">Manage and upload your products</p>
                        </div>
                        <button
                          onClick={() => setShowUploadForm(!showUploadForm)}
                          className="bg-gradient-to-r from-primary-blue to-blue-600 hover:from-blue-600 hover:to-primary-blue text-white px-6 py-3 rounded-xl font-semibold transition-all flex items-center gap-2 shadow-lg hover:shadow-xl transform hover:-translate-y-0.5"
                        >
                          <svg className="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 4v16m8-8H4" />
                          </svg>
                          Upload New Product
                        </button>
                      </div>

                      {/* Upload Form */}
                      {showUploadForm && (
                        <div className="bg-gradient-to-br from-accent-cream to-white p-8 rounded-2xl mb-8 border-2 border-dashed border-peachy-pink shadow-lg">
                          <h3 className="text-xl font-semibold mb-4 text-qblack">Upload Product for Verification</h3>
                          <form onSubmit={handleSubmitProduct}>
                            <div className="grid grid-cols-1 md:grid-cols-2 gap-5 mb-5">
                              <InputCom
                                placeholder="Product Name"
                                label="Product Name*"
                                name="name"
                                type="text"
                                value={productData.name}
                                inputHandler={handleInputChange}
                                inputClasses="h-[50px]"
                              />
                              <InputCom
                                placeholder="Category"
                                label="Category*"
                                name="category"
                                type="text"
                                value={productData.category}
                                inputHandler={handleInputChange}
                                inputClasses="h-[50px]"
                              />
                            </div>

                            <div className="grid grid-cols-1 md:grid-cols-2 gap-5 mb-5">
                              <InputCom
                                placeholder="0.00"
                                label="Price*"
                                name="price"
                                type="number"
                                value={productData.price}
                                inputHandler={handleInputChange}
                                inputClasses="h-[50px]"
                              />
                              <InputCom
                                placeholder="Stock Quantity"
                                label="Stock*"
                                name="stock"
                                type="number"
                                value={productData.stock}
                                inputHandler={handleInputChange}
                                inputClasses="h-[50px]"
                              />
                            </div>

                            <div className="mb-5">
                              <label className="input-label text-qgray capitalize text-[13px] font-normal block mb-2">
                                Description*
                              </label>
                              <textarea
                                name="description"
                                value={productData.description}
                                onChange={handleInputChange}
                                placeholder="Product Description"
                                className="w-full h-[120px] border border-[#EDEDED] px-5 py-3 focus:outline-none focus:ring-1 focus:ring-customBlue"
                                required
                              />
                            </div>

                            {/* Image Upload */}
                            <div className="mb-5">
                              <label className="input-label text-qgray capitalize text-[13px] font-normal block mb-2">
                                Product Images*
                              </label>
                              <div className="border-2 border-dashed border-gray-300 rounded-lg p-6 text-center">
                                <input
                                  ref={imageInputRef}
                                  type="file"
                                  multiple
                                  accept="image/*"
                                  onChange={handleImageUpload}
                                  className="hidden"
                                />
                                <button
                                  type="button"
                                  onClick={() => imageInputRef.current.click()}
                                  className="bg-gray-200 text-gray-700 px-6 py-3 rounded-lg font-semibold hover:bg-gray-300 transition-all"
                                >
                                  Choose Images
                                </button>
                                <p className="text-sm text-gray-500 mt-2">Upload up to 5 images</p>
                              </div>

                              {/* Image Preview */}
                              {productImages.length > 0 && (
                                <div className="grid grid-cols-5 gap-4 mt-4">
                                  {productImages.map((img, index) => (
                                    <div key={index} className="relative">
                                      <img
                                        src={img}
                                        alt={`Product ${index + 1}`}
                                        className="w-full h-24 object-cover rounded-lg"
                                      />
                                      <button
                                        type="button"
                                        onClick={() => removeImage(index)}
                                        className="absolute -top-2 -right-2 bg-red-500 text-white rounded-full w-6 h-6 flex items-center justify-center hover:bg-red-600"
                                      >
                                        ×
                                      </button>
                                    </div>
                                  ))}
                                </div>
                              )}
                            </div>

                            <div className="flex gap-4">
                              <button
                                type="submit"
                                className="bg-gradient-to-r from-primary-blue to-blue-600 hover:from-blue-600 hover:to-primary-blue text-white px-8 py-3 rounded-xl font-semibold transition-all shadow-lg hover:shadow-xl transform hover:-translate-y-0.5"
                              >
                                Submit for Verification
                              </button>
                              <button
                                type="button"
                                onClick={() => setShowUploadForm(false)}
                                className="bg-gray-100 text-gray-700 px-8 py-3 rounded-xl font-semibold hover:bg-gray-200 transition-all"
                              >
                                Cancel
                              </button>
                            </div>
                          </form>
                        </div>
                      )}

                      {/* Products List */}
                      <div className="space-y-4">
                        {products.length === 0 ? (
                          <div className="text-center py-12 bg-gray-50 rounded-lg">
                            <svg
                              className="w-16 h-16 mx-auto text-gray-400 mb-4"
                              fill="none"
                              stroke="currentColor"
                              viewBox="0 0 24 24"
                            >
                              <path
                                strokeLinecap="round"
                                strokeLinejoin="round"
                                strokeWidth={2}
                                d="M20 13V6a2 2 0 00-2-2H6a2 2 0 00-2 2v7m16 0v5a2 2 0 01-2 2H6a2 2 0 01-2-2v-5m16 0h-2.586a1 1 0 00-.707.293l-2.414 2.414a1 1 0 01-.707.293h-3.172a1 1 0 01-.707-.293l-2.414-2.414A1 1 0 006.586 13H4"
                              />
                            </svg>
                            <p className="text-gray-500 text-lg">No products yet. Upload your first product!</p>
                          </div>
                        ) : (
                          products.map((product) => (
                            <div
                              key={product.id}
                              className="bg-white border-2 border-gray-100 rounded-2xl p-6 hover:shadow-2xl hover:border-primary-blue/20 transition-all transform hover:-translate-y-1"
                            >
                              <div className="flex gap-6">
                                {product.images.length > 0 && (
                                  <img
                                    src={product.images[0]}
                                    alt={product.name}
                                    className="w-32 h-32 object-cover rounded-lg"
                                  />
                                )}
                                <div className="flex-1">
                                  <div className="flex justify-between items-start mb-2">
                                    <h3 className="text-xl font-bold text-qblack">{product.name}</h3>
                                    {getStatusBadge(product.verificationStatus)}
                                  </div>
                                  <p className="text-gray-600 mb-2">{product.description}</p>
                                  <div className="flex gap-6 text-sm text-gray-500">
                                    <span>Category: <strong>{product.category}</strong></span>
                                    <span>Price: <strong>${product.price}</strong></span>
                                    <span>Stock: <strong>{product.stock}</strong></span>
                                  </div>
                                  <p className="text-xs text-gray-400 mt-2">
                                    Submitted: {new Date(product.submittedAt).toLocaleDateString()}
                                  </p>
                                </div>
                              </div>
                            </div>
                          ))
                        )}
                      </div>
                    </div>
                  )}

                  {/* Messages Tab */}
                  {activeTab === "messages" && (
                    <div>
                      <div className="mb-6">
                        <h2 className="text-2xl font-bold text-qblack mb-2">Messages</h2>
                        <p className="text-sm text-medium-grey">Communicate with potential buyers</p>
                      </div>

                      <div className="space-y-4">
                        {messages.length === 0 ? (
                          <div className="text-center py-12 bg-gray-50 rounded-lg">
                            <svg
                              className="w-16 h-16 mx-auto text-gray-400 mb-4"
                              fill="none"
                              stroke="currentColor"
                              viewBox="0 0 24 24"
                            >
                              <path
                                strokeLinecap="round"
                                strokeLinejoin="round"
                                strokeWidth={2}
                                d="M8 10h.01M12 10h.01M16 10h.01M9 16H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 01-2 2h-5l-5 5v-5z"
                              />
                            </svg>
                            <p className="text-gray-500 text-lg">No messages yet</p>
                          </div>
                        ) : (
                          messages.map((msg) => (
                            <div
                              key={msg.id}
                              className={`bg-white rounded-xl p-6 border-2 transition-all hover:shadow-lg ${msg.unread ? 'border-primary-blue bg-blue-50/30' : 'border-gray-100'
                                }`}
                            >
                              <div className="flex items-start gap-4">
                                <div className="w-12 h-12 rounded-full bg-gradient-to-r from-primary-blue to-blue-500 text-white flex items-center justify-center font-bold text-lg flex-shrink-0">
                                  {msg.sender.charAt(0)}
                                </div>
                                <div className="flex-1">
                                  <div className="flex items-center justify-between mb-2">
                                    <h3 className="font-bold text-qblack">{msg.sender}</h3>
                                    <div className="flex items-center gap-2">
                                      <span className="text-xs text-gray-500">{msg.time}</span>
                                      {msg.unread && (
                                        <span className="bg-red-500 text-white text-xs px-2 py-1 rounded-full font-semibold">New</span>
                                      )}
                                    </div>
                                  </div>
                                  <p className="text-medium-grey mb-3">{msg.message}</p>
                                  <button className="bg-primary-blue text-white px-4 py-2 rounded-lg text-sm font-semibold hover:bg-blue-600 transition-colors">
                                    Reply
                                  </button>
                                </div>
                              </div>
                            </div>
                          ))
                        )}
                      </div>
                    </div>
                  )}
                </div>
              </div>
            </div>
          </div>
        </div>
    </Layout>
  );
}
